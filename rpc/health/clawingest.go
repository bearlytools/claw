// DO NOT EDIT
// This package is autogenerated and should not be modified except by the clawc compiler.

package health

import (
    "context"
    "fmt"

    "github.com/bearlytools/claw/clawc/languages/go/clawiter"
    "github.com/bearlytools/claw/clawc/languages/go/field"
)

// Ensure imports are used.
var _ context.Context
var _ = fmt.Errorf
var _ = field.FTBool


// Ingest populates the struct from a Walker with options.
// This is the inverse of Walk().
func (x *HealthCheckRequest) Ingest(ctx context.Context, walk clawiter.Walker, opts ...clawiter.IngestOption) error {
    // Apply options
    var o clawiter.IngestOptions
    for _, opt := range opts {
        var err error
        o, err = opt(o)
        if err != nil {
            return err
        }
    }

    // Convert Walker to iter.Seq for TokenStream
    seq := func(yield func(clawiter.Token) bool) {
        walk(yield)
    }
    ts := clawiter.NewTokenStream(seq)
    defer ts.Close()
    return x.XXXIngestFrom(ctx, ts, o)
}

// XXXIngestFrom is for internal use - ingests from a shared token stream.
func (x *HealthCheckRequest) XXXIngestFrom(ctx context.Context, ts *clawiter.TokenStream, opts clawiter.IngestOptions) error {
    tok, ok := ts.Next()
    if !ok {
        return fmt.Errorf("expected TokenStructStart, got EOF")
    }
    if tok.Kind != clawiter.TokenStructStart {
        return fmt.Errorf("expected TokenStructStart, got %v", tok.Kind)
    }

    for {
        tok, ok = ts.Next()
        if !ok {
            return fmt.Errorf("unexpected EOF in HealthCheckRequest")
        }

        if tok.Kind == clawiter.TokenStructEnd {
            return nil
        }

        if tok.Kind != clawiter.TokenField {
            return fmt.Errorf("expected TokenField, got %v", tok.Kind)
        }

        switch tok.Name {
        case "Service":
            x.SetService(tok.String())
        default:
            if opts.IgnoreUnknownFields {
                if err := clawiter.SkipValue(ts, tok); err != nil {
                    return err
                }
                continue
            }
            return fmt.Errorf("unknown field: %s", tok.Name)
        }
    }
}

// Ingest populates the struct from a Walker with options.
// This is the inverse of Walk().
func (x *HealthCheckResponse) Ingest(ctx context.Context, walk clawiter.Walker, opts ...clawiter.IngestOption) error {
    // Apply options
    var o clawiter.IngestOptions
    for _, opt := range opts {
        var err error
        o, err = opt(o)
        if err != nil {
            return err
        }
    }

    // Convert Walker to iter.Seq for TokenStream
    seq := func(yield func(clawiter.Token) bool) {
        walk(yield)
    }
    ts := clawiter.NewTokenStream(seq)
    defer ts.Close()
    return x.XXXIngestFrom(ctx, ts, o)
}

// XXXIngestFrom is for internal use - ingests from a shared token stream.
func (x *HealthCheckResponse) XXXIngestFrom(ctx context.Context, ts *clawiter.TokenStream, opts clawiter.IngestOptions) error {
    tok, ok := ts.Next()
    if !ok {
        return fmt.Errorf("expected TokenStructStart, got EOF")
    }
    if tok.Kind != clawiter.TokenStructStart {
        return fmt.Errorf("expected TokenStructStart, got %v", tok.Kind)
    }

    for {
        tok, ok = ts.Next()
        if !ok {
            return fmt.Errorf("unexpected EOF in HealthCheckResponse")
        }

        if tok.Kind == clawiter.TokenStructEnd {
            return nil
        }

        if tok.Kind != clawiter.TokenField {
            return fmt.Errorf("expected TokenField, got %v", tok.Kind)
        }

        switch tok.Name {
        case "Status":
            if len(tok.Bytes) > 0 {
                x.SetStatus(ServingStatusByName[tok.String()])
            } else {
                x.SetStatus(ServingStatus(tok.Uint8()))
            }
        default:
            if opts.IgnoreUnknownFields {
                if err := clawiter.SkipValue(ts, tok); err != nil {
                    return err
                }
                continue
            }
            return fmt.Errorf("unknown field: %s", tok.Name)
        }
    }
}

