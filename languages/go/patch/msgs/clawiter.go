// DO NOT EDIT
// This package is autogenerated and should not be modified except by the clawc compiler.

package msgs

import (
    "context"
    "iter"
    "math"

    "github.com/bearlytools/claw/clawc/languages/go/clawiter"
    "github.com/bearlytools/claw/clawc/languages/go/field"
    
)

// Ensure imports are used.
var _ = math.Float32bits
var _ context.Context


// Walk returns an iterator that emits tokens for serialization.
// This walks all fields including nested structs and lists.
func (x Op) Walk(ctx context.Context) iter.Seq[clawiter.Token] {
    return func(yield func(clawiter.Token) bool) {
        if !yield(clawiter.Token{Kind: clawiter.TokenStructStart, Name: "Op"}) {
            return
        }
        // Field 0: FieldNum
        {
            v := x.FieldNum()
            tok := clawiter.Token{Kind: clawiter.TokenField, Name: "FieldNum", Type: field.FTUint16}
            tok.SetUint16(uint16(v))
            if !yield(tok) {
                return
            }
        }
        // Field 1: Type
        {
            v := x.Type()
            tok := clawiter.Token{Kind: clawiter.TokenField, Name: "Type", Type: field.FTUint8}
            tok.SetUint8(uint8(v))
            tok.IsEnum = true
            tok.EnumGroup = "OpType"
            tok.EnumName = OpTypeByValue[uint8(v)]
            if !yield(tok) {
                return
            }
        }
        // Field 2: Index
        {
            tok := clawiter.Token{Kind: clawiter.TokenField, Name: "Index", Type: field.FTInt32}
            tok.SetInt32(x.Index())
            if !yield(tok) {
                return
            }
        }
        // Field 3: Data
        if !yield(clawiter.Token{Kind: clawiter.TokenField, Name: "Data", Type: field.FTBytes, Bytes: x.Data()}) {
            return
        }

        if !yield(clawiter.Token{Kind: clawiter.TokenStructEnd, Name: "Op"}) {
            return
        }
    }
}

// Walk returns an iterator that emits tokens for serialization.
// This walks all fields including nested structs and lists.
func (x Patch) Walk(ctx context.Context) iter.Seq[clawiter.Token] {
    return func(yield func(clawiter.Token) bool) {
        if !yield(clawiter.Token{Kind: clawiter.TokenStructStart, Name: "Patch"}) {
            return
        }
        // Field 0: Version
        {
            v := x.Version()
            tok := clawiter.Token{Kind: clawiter.TokenField, Name: "Version", Type: field.FTUint8}
            tok.SetUint8(uint8(v))
            if !yield(tok) {
                return
            }
        }
        // Field 1: Ops
        {
            list := x.OpsList(ctx)
            listLen := list.Len()
            if listLen == 0 {
                if !yield(clawiter.Token{Kind: clawiter.TokenField, Name: "Ops", Type: field.FTListStructs, StructName: "Op", IsNil: true}) {
                    return
                }
            } else {
                if !yield(clawiter.Token{Kind: clawiter.TokenField, Name: "Ops", Type: field.FTListStructs, StructName: "Op"}) {
                    return
                }
                if !yield(clawiter.Token{Kind: clawiter.TokenListStart, Name: "Ops", Type: field.FTListStructs, Len: listLen}) {
                    return
                }
                for i := 0; i < listLen; i++ {
                    item := x.OpsGet(ctx, i)
                    for tok := range item.Walk(ctx) {
                        if !yield(tok) {
                            return
                        }
                    }
                }
                if !yield(clawiter.Token{Kind: clawiter.TokenListEnd, Name: "Ops"}) {
                    return
                }
            }
        }

        if !yield(clawiter.Token{Kind: clawiter.TokenStructEnd, Name: "Patch"}) {
            return
        }
    }
}

